# SPEC

## 現在の実装状態
- 本リポジトリは教科書プロジェクトの骨組みを提供する。
- `logic_tower` / `math_tower` / `physics_tower` / `db_tower` に章構成を配置済み。
- 基本方針はスケルトン起点だが、`physics_tower` は `physics_tower/PROGRESS.md` のキュー順に本文拡充（教科書化）を進める。

- `logic_tower` は本線のv1.0品質更新と、`90_essays` の発展・応用65記事の本文化を完了済み。今後の追加は `logic_tower/PROGRESS.md` に新しいキューを登録してから行う。

## 運用ルール
- 目次タワーにない章やファイルは勝手に追加しない。
- 入口ページは `00_overview.md` 優先、なければ `README.md` を使用する。
- ディレクトリのみ指定の場所は `README.md` を入口として先に配置し、必要なら `.gitkeep` を補助的に使う。
- リンクは相対パスで管理し、リンク切れを作らない。
- PR作成後はレビュー指摘と基本チェックを反映し、`SPEC.md` と実装内容に矛盾がないことを確認できた場合は **必ず同一タスク内で `main` へマージ完了まで実施**する（PR作成のみで終了しない）。

### Physicsタワー本文の数式規約
- 数式は原則として独立行のブロック数式（`$$...$$`）で記述し、文章中のTeX埋め込みを避ける。
- 上記の数式ルールは全セクション共通とし、演習問題・演習解答・箇条書き・章末まとめでも独立ブロックを維持する。
- 行列を扱う数式は `\begin{pmatrix}...\end{pmatrix}` を同一ブロック数式内で完結させ、閉じタグ欠落や途中分断を禁止する。
- `\end{pmatrix}` / `\end{vmatrix}` の直後に `=` が続く場合は同一行（例: `...\end{pmatrix}=...`）で記述し、Markdown見出し誤解釈を避ける。
- 数式中の `=` の直前で改行しない（`=` は前行末尾へ置く）。

### Physicsタワー執筆フロー（引き継ぎ可能な運用）
- 本文執筆は毎回 **1ファイルだけ** 実施する（`PROGRESS.md` の先頭 `TODO` または `DOING` 優先）。
- ユーザーが対象ファイルまたは件数を明示した一括バッチでは、その指定範囲を優先し、意味のあるコミット単位に分けて全対象を同一品質で検査する。
- ユーザー指示が「次をお願いします」または「progressmdをみて続きに当たるmd教科書を作る」の場合も、同じルールで `PROGRESS.md` に従って1件のみ本文化する。
- 新しい章（新フォルダ）に突入するタイミングでは、先にその章の学習導線を設計し、**子mdを3ファイル以上スケルトン作成**してから本文執筆を開始する。
- 上記の「子mdを3件以上」ルールは `physics_tower` 配下の**すべての章フォルダ**（例: `01_dynamics`, `02_electromagnetism`, `03_analytical_mechanics` ...）に適用し、各章のサブフォルダで不足があれば本文化前にスケルトンを補完する。
- 新章の `README.md` は単なるリンク集にせず、初心者向けの目的・読み方・主要トピック・子md案内を含む本文として整備する。
- 本文化は「導入→導出→例題→誤りやすい点→まとめ」の流れを基本とし、次回の担当AIが継続しやすいように、`PROGRESS.md` の `notes` に実施内容を明記する。
- 上記フローと矛盾する古い運用記述は採用しない（常にこの節を最新の基準とする）。

### Physicsタワーのモデル化・限界記述

今後作る各章・記事は、題材に応じて次へ答える。既存完成記事を完全準拠のためだけに一括改稿しない。

1. なぜこの概念が必要か。
2. 何を対象にするか。
3. 何を省略・近似するか。
4. どの原理から導かれるか。
5. 前後の理論とどうつながるか。
6. 何を説明・計算できるか。
7. どの条件で成立するか。
8. どこで破綻するか。
9. どの応用へつながるか。
10. 次に何を学ぶべきか。

- Physics Tower全体は、現実→対象・scale→model・変数・近似→原理→計算・検算・実験→現象・工学・製品→適用限界→次理論、の循環で構成する。
- 解析力学は Newton 力学と同じ運動の表現変更として扱い、単純問題では Newton 形式が短い場合も明記する。
- 熱力学は微視的構造を仮定しない独立した巨視理論、統計力学は微視的理由と物質固有値を説明する理論として役割を分ける。
- 古典統計の適用域と量子統計が必要な低温・高密度・同種粒子領域を区別し、量子力学本編の状態・時間発展を先取りしない。

### 量子力学章の統一規約

- `07_quantum_mechanics` は古典論の破綻実験から始め、state、operator、measurement、Hamiltonian time evolutionを一本の予測規則として組み立てる。
- 一粒子位置波動関数、spinor、多粒子configuration-space wave function、一般state vector、density operatorを区別する。
- 時間発展、測定確率、測定後state、decoherence、解釈問題を別の論点として扱い、意識によるcollapseを標準理論の確定事項としない。
- Fourier規約は `exp(-ipx/ℏ)` を位置→運動量、時間発展は `exp(-iEt/ℏ)`、確率currentは `j=(ℏ/2mi)(ψ*∇ψ-ψ∇ψ*)` に統一する。
- canonical relationは `[q_i,p_j]=iℏδ_ij` とし、Poisson括弧からcommutatorへの対応を量子化の一意な万能recipeとは扱わない。
- spinを古典的自転と説明せず、軌道角運動量と内部自由度を区別する。
- 量子力学はstate空間と交換対称性、統計力学は熱平衡の占有分布、電磁気学は電位・電荷・電流を正本とする。
- 相対論的量子論は境界の提示に留め、Lorentz構造の詳細は `06_relativity`、生成消滅とfield quantizationは `08_elementary_particle` へ保留する。

### 電磁気学章の統一規約

- `physics_tower/02_electromagnetism/` は、真空中の全電荷・全電流による記述と、物質中の自由電荷・自由電流による巨視的記述を区別する。
- `D=εE`、`B=μH`、`J=σE` は普遍的なMaxwell方程式ではなく、線形・等方・局所などの条件を持つ構成方程式として扱う。
- 複素振幅の時間依存は同章内で `exp(-iωt)` に統一し、別規約を引用する場合は符号差を明記する。
- 境界条件では法線を媒質1から2へ向け、自由表面電荷・自由表面電流を右辺に置く。
- 電磁場の単純な物質中エネルギー密度式は、線形・非分散・損失なしなどの適用条件を明記する。
- 回路理論は電磁場の空間分布を端子量へ圧縮した有効モデルとして扱い、R・C・Lの材料・形状依存、Kirchhoff則の由来、集中定数近似の条件を明記する。
- 回路の電圧・電流は受動符号規約を基本とし、起電力と電圧降下、実効値と最大値、複素振幅と実時間量を区別する。
- `02_electromagnetism` は回路量の由来と大学受験〜工学初歩への接続、`10_circuits` は回路網・フィルタ・能動素子・半導体・実装・計測の詳細を正本とする。
- 半導体は二段階で扱う。電磁気学では電位・Poisson方程式・電荷・容量・電流・RC・電力を正本とし、バンド構造・Fermi準位・キャリア統計・トンネル・量子閉じ込めの導出は量子力学・統計力学へ保留する。
- 電子電荷は `q_e=-e`、電子の静電ポテンシャルエネルギーは `U_e=-eφ` とし、電子の移動方向と慣用電流を区別する。
- 現代製品は物理法則→デバイス→回路→チップ→パッケージ→システム→データセンターの階層で整理し、企業情報には公式一次資料と確認日を付す。

### Logicタワー執筆フロー（Physics運用の準拠版）
- 本文執筆は原則 **1ファイルだけ** 実施する（`logic_tower/PROGRESS.md` の先頭 `TODO` または `DOING` 優先）。ユーザーが件数を明示した一括バッチでは、その指定範囲を優先し、全対象を同一品質基準で検査する。
- 新しい章（新フォルダ）に突入するタイミングでは、先に章の学習導線を設計し、**子mdを3ファイル以上スケルトン作成**してから本文執筆を開始する。
- 入口ページは `00_overview.md` を優先し、存在しないフォルダでは `README.md` を入口として整備する。
- 本文化後は `logic_tower/PROGRESS.md` の該当行を更新し、`last_updated` と `notes` を必ず残す。
- Logicタワーの本文は初心者向けを最優先とし、各ページで「直観→定義→例→つまずきやすい点→確認」の順を基本テンプレートとして採用する。


### Logicタワー図解ポリシー（Mermaid）
- Logicタワーの図解は **ASCIIアートを使わず**、Markdown内の ` ```mermaid ` コードブロックで記述する。
- 本文化する各ページには、原則として **1記事1図以上** のMermaid図を入れる（導入図・関係図・手順図のいずれか）。
- 図の直前に「この図で何を読み取るか」を1〜2行で明記し、図だけで終わらせない。
- 執筆時は `logic_tower/PROGRESS.md` の運用ルールも同時に参照し、図解要件の有無を自己確認してから `DONE` 更新する。
- 論理記号を扱う箇所は、可能な限りTeX記法（例: `$\lnot P$`, `$P \land Q$`, `$P \to Q$`, `$\forall x$`, `$\exists x$`）を優先し、ASCII代替（`->`, `&&` など）のみで済ませない。


### Logicタワー発展・応用記事の運用
- 本線は `00_orientation` から `05_modal_and_nonclassical` までとし、既存番号を維持する。
- 発展・応用は `logic_tower/90_essays/` 配下のカテゴリ別サブディレクトリで管理する。
- 新規子ページは、概要・論点・前提・誤解防止の注意を持つスケルトンから開始し、完成本文と区別する。
- v1.1拡張キューは全65記事を本文化済み。今後は新しい拡張キューが追加された場合に限り、先頭から進める。件数指定のない通常作業は1回1ファイル、明示された一括バッチは指定件数を対象とする。
- 既存の完成記事と題材が重なる場合は正本を移動・複製せず、カテゴリREADMEや発展記事から相対リンクする。
- 各本文化ページに、種別・前提・難易度・読了目安を表示する。
- 現代研究・AI・製品を扱う記事では、定理・実証結果・解釈・仮説を区別し、一次資料の確認日と前提を記録する。
- 全体の中心軸を、表現力・推論可能性・計算可能性・実用性のトレードオフとする。
