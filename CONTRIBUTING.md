# CONTRIBUTING

このリポジトリは教科書骨組みを保つため、命名とリンク整合性を重視します。
章追加時は既存の階層方針を守り、最小差分で更新してください。

## 命名規則
- 章フォルダ: `NN_topic_name` 形式
- 入口ファイル: `00_overview.md` があれば優先、なければ `README.md`
- 演習やレッスン下書きは `templates/` を利用

## 記述ルール
- まずは H1、概要、扱うこと、ナビゲーションのみ
- 図や画像は `assets/` 配下に配置（必要時に新設）
- 相対リンクを使い、リンク切れを避ける
